//https://www.npmjs.com/package/node-geocoder
//Utiliser geoCoder pour convertir une adresse en coordonnées
export interface evenement
{
    titre? : string,
    organisation? : string,
    date? : Date,
    desciption? : string,
    endroit? : string,
    adresse? : string,
    codePostal? : string,
    latitude? : number,
    longitude? : number
}